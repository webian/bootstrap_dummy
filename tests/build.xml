<?xml version="1.0"?>
<project name="main" basedir="." default="help"  description="Fetches the data from a remote website">

	<!-- FLAGS -->
	<property name="dryRun" value="${dryRun}"/>

	<!--
		==============================================
		Default entry point
		==============================================
	-->
	<target name="help">
		<echo>Usage of this Phing:</echo>
		<echo/>
		<echo>phing behat - fetch the database and build it again locally for TYPO3 6.0</echo>
		<echo/>

		<echo>---------------------------------------------</echo>
		<echo>Possible option</echo>
		<echo>---------------------------------------------</echo>
		<echo>-DdryRun=true - will display the command to be executed</echo>
	</target>
</project>
